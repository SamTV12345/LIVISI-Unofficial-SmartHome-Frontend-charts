apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "livisi-charts.fullname" . }}-test-connection"
  labels:
    {{- include "livisi-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "livisi-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
