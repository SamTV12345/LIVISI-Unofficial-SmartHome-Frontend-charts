apiVersion: v1
kind: Service
metadata:
  name: {{ include "livisi-charts.fullname" . }}
  labels:
    {{- include "livisi-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "livisi-charts.selectorLabels" . | nindent 4 }}
